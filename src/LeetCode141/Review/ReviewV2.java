package LeetCode141.Review;

/**
 * Definition for singly-linked list.
 * class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) {
 *         val = x;
 *         next = null;
 *     }
 * }
 */
public class ReviewV2 {
    // We can use two pointers: slow and fast
    // If there is a cycle in some point they will be at the same position
    // If there is not a cycle the faster will be at null
    public boolean hasCycle(ListNode head) {

    }
}
